void main() {
  final status = true;
  // final status = 15 > 10;
  // String name = "Fauzi";
  // final status = 15 > 10;
  // final status = name.length > 10;
  print(status && false);
  print(status || false);
  print(!status);
}
